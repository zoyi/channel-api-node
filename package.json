{
  "name": "channel-api-node",
  "version": "0.1.0",
  "scripts": {
    "test": "mocha --opts tests/mocha.opts tests/**/*-test.ts",
    "build": "rm -rf lib && tsc -p . && rm -fr lib/tests && mv lib/src/** lib/ && rm -fr lib/src",
    "prepublish": "npm run build"
  },
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "files": [
    "lib/"
  ],
  "devDependencies": {
    "@types/chai": "^3.4.34",
    "@types/mocha": "^2.2.32",
    "@types/node": "^6.0.42",
    "chai": "^3.5.0",
    "mocha": "^3.1.0",
    "mocha-clean": "^1.0.0",
    "nock": "^9.0.2",
    "ts-node": "^1.3.0",
    "typescript": "^2.0.3"
  },
  "dependencies": {
    "request": "^2.79.0",
    "request-promise": "^4.1.1"
  }
}
